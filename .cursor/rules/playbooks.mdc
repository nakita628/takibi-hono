---
description: Step-by-step playbooks for the recurring tasks here — adding a config option, fixing a generator bug, porting from hono-takibi, a vendor extension request, checking a change in the fixture, clearing a lint failure, adding a lint rule, bumping effect or oas-truth, and releasing. Read this when starting any of those.
alwaysApply: false
---

# Playbooks

Every playbook ends the same way: `pnpm fix`, then `pnpm check` and `pnpm test`, all green, then
commit.

## Add a config option

1. Write the failing tests first: validation in `src/config/index.test.ts`, output in the suite of
   the layer that owns it (`src/core/index.test.ts`, `src/generator/*.test.ts`).
2. Add the field to `ConfigSchema` in `src/config/index.ts`, with a check whose message names what
   is expected, and to `TakibiHonoConfig` / `resolveLayout` in `src/core/layout.ts` if it affects
   paths.
3. Implement it in `core/` (the Effect program) and `generator/` / `helper/` (the pure builders),
   respecting the layering. Both entry points reach it through `hono()`; never special-case one.
4. Document it in the README (`Full Config`), both copies.

## Fix a generator bug

1. Reproduce it as a failing test at the smallest document that shows it, asserting the exact
   generated source.
2. Find the layer that owns the mistake — parsing and Components (oas-truth), a schema expression
   (schema-to-library), a code builder (`generator/`), composition (`helper/`), the program
   (`core/`), the merge (`merge/`) — and fix it there, not downstream. A mistake in oas-truth or
   schema-to-library is fixed upstream, never by string-patching its output here.
3. Check whether the other four schema libraries, and `openapi: false`, have the same mistake.

## Port a feature from hono-takibi

1. Read the hono-takibi implementation and its tests end to end before writing anything.
2. List what differs here: hono-openapi `describeRoute` + validators instead of
   `@hono/zod-openapi`, five schema libraries through oas-truth, handler files that merge into the
   user's code. Adapt; do not copy a behavior that would delete or overwrite user code.
3. Port the tests first and make sure they fail against the current code before the
   implementation lands.
4. Say in the commit body what was taken as-is and what changed and why.

## A request to support a vendor extension (`x-*`)

1. Classify it with the architecture rule's three layers.
2. A or B: it belongs in schema-to-library (and oas-truth's `Schema` type), not here — takibi-hono
   only passes it through. Add a transparency test here only if the pass-through itself changes.
3. C (code injection): decline, citing the architecture rule. Do not add a flag, an option or an
   allowlist for it.

## Check a change in the fixture

For a change to the Vite plugin, the handler / app output or the merge:

1. `vp run takibi-hono#build`.
2. Regenerate: `node ../../packages/takibi-hono/dist/index.js` from `fixtures/vite-plugin`, and
   read the diff of `src/`.
3. `pnpm -F fixture-vite-plugin dev`, request a route, edit a handler body, restart, and confirm
   the body survived and the terminal shows no warning. Revert any hand edit before committing.

## Clear a lint failure

1. Run `pnpm check` and read the rule name in the message.
2. Try `pnpm fix` first — formatting and import order are autofixable.
3. Otherwise **change the code, not the rule.** The rules encode the design: an assertion ban means
   narrow with a guard; a `no-restricted-imports` message means the dependency direction is wrong;
   `prefer-each` means `it.each`.
4. Only if the rule genuinely cannot hold, add `// oxlint-disable-next-line rule-name -- reason`
   on that line, and say in the report that you added a suppression and why.

## Add a lint rule

1. Check its category; correctness / suspicious / perf are already errors.
2. Add it to the `lint` section of `packages/takibi-hono/vite.config.ts` in its section, with the
   reason.
3. Run `pnpm check` and fix every finding in the code.

## Bump effect

1. Find the release candidate oas-truth's schema-to-library now uses; that is the target.
2. Change `effect` and `@effect/platform-node` in `packages/takibi-hono/package.json` (both exact)
   and the `@effect/platform-node-shared` override in `pnpm-workspace.yaml` together, then
   `pnpm install`.
3. `pnpm check`. The RC moves the Schema and CLI APIs, so expect errors in `config/` and `cli/`.
4. `pnpm test`, and read every behavior difference rather than adjusting an assertion to match.
5. Say in the commit body which upstream changes forced code changes.

## Bump oas-truth or hono-takibi

1. Change the version; if it is younger than pnpm's minimum release age, add it to
   `minimumReleaseAgeExclude` in `pnpm-workspace.yaml` (and drop entries that no longer need it).
2. `pnpm install`, `pnpm test`. A changed expectation is a changed generated output: read it and
   decide whether it is right before updating the test.
3. If oas-truth moved schema-to-library to a new Effect RC, do the effect bump in the same change.

## Release (only when asked)

1. Confirm `main` is green.
2. Bump `version` in `packages/takibi-hono/package.json` only (semver; while the version is `0.x`,
   a breaking change to the config, the CLI or the generated output is a minor bump).
3. `pnpm check` and `pnpm test`, then commit `chore: release takibi-hono <version>` and push to
   `main`.
4. Watch Test, then `release`: it tags `v<version>`, publishes to npm and creates the GitHub
   release from that commit. Never publish or tag by hand.
5. If a job fails, fix the cause and re-run the failed jobs. To fix a published release, bump
   again — never reuse a version.
